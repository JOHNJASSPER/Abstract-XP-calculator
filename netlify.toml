[build]
  publish = "."
  command = ""

# Redirects rule for SPA (if we expand later) or just clean URLs
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
